fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
slowapi>=0.1.9

# LangChain — compatible set
langchain>=0.2.16
langchain-community>=0.2.16
langchain-core>=0.2.38

# Active LLM provider
langchain-anthropic>=0.1.23

# Embeddings (free, local — no API key needed for small corpora like a resume)
langchain-huggingface>=0.0.3
sentence-transformers>=3.0.0

# Vector store
chromadb>=0.5.0
langchain-chroma>=0.1.0

# Commented-out provider deps — install when re-enabling:
# ollama>=0.2.0                 # for Ollama/local models
# pypdf>=4.2.0                  # for PDF resume ingestion

python-dotenv>=1.0.1