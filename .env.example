# Copy to .env — never commit this file!

# ── Active ────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...       # still needed for embeddings (cheap)

# ── Uncomment when re-enabling other providers ────
# OLLAMA_BASE_URL=http://localhost:11434
